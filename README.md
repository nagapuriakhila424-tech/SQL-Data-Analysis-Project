# SQL-Data-Analysis-Project
SQL Data Analysis project using Python, Pandas, and SQLite. The project covers data cleaning, database creation, SQL query execution, and business insight generation using SELECT, WHERE, GROUP BY, ORDER BY, COUNT, SUM, and AVG operations to support data-driven decision-making.
